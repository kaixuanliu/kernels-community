[general]
name = "trimul-gpumode"
version = 1
edition = 5
license = "Apache-2.0"
backends = [
    "cuda",
    "rocm",
    "xpu",
]

[general.hub]
repo-id = "kernels-community/trimul-gpumode"

[torch-noarch]

[kernel]
